let log = new Array(5);
log = [];
for(let i=0;i<log.length;i++){
        console.log(`Hello ${log[i]}`);
    }
    if(log.length == 0){
        console.log("We need to find some uers");
    }
    